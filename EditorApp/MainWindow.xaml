<Window x:Class="EditorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EditorApp"
        mc:Ignorable="d"
        Title="Config Editor" Height="500" Width="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Version:" Width="60"/>
            <TextBox Text="{Binding Version}" Width="200"/>
            <TextBlock Text="Uptime:" Margin="20,0,0,0"/>
            <TextBox Text="{Binding UpTime}" Width="200"/>
        </StackPanel>

        <DataGrid x:Name="AccountsDataGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding Accounts}"
                  AutoGenerateColumns="False"
                  SelectionMode="Extended"
                 SelectionUnit="FullRow">
            

            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled}" />
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="User Name" Binding="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Password" Binding="{Binding Password}" />
                <DataGridTextColumn Header="Password Type" Binding="{Binding PasswordType}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Generate Passwords" Width="150" Margin="5" Click="GeneratePasswords_Click"/>
            <Button Content="Save" Width="100" Margin="5" Click="Save_Click"/>
        </StackPanel>
                  

    </Grid>
</Window>
